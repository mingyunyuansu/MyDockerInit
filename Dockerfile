FROM ubuntu:latest
CMD apt update && apt upgrade && apt install -y curl wget python3 git gcc g++ cmake libtool autoconf zsh neovim vim && sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)" && git clone --depth=1 https://github.com/romkatv/powerlevel10k.git ${ZSH_CUSTOM:-$HOME/.oh-my-zsh/custom}/themes/powerlevel10k && sed -i "s/ZSH_THEME=.*/ZSH_THEME='powerlevel10k/powerlevel10k'/g" && git clone https://github.com/zsh-users/zsh-autosuggestions ${ZSH_CUSTOM:-~/.oh-my-zsh/custom}/plugins/zsh-autosuggestions && sed -i "s/plugins=*/plugins=(git zsh-autosuggestions)"
